var name = "Alex";
name = "Rick";

console.log(name);

var action = " is reading a book";

// Concatenation with '+' operator
console.log(name + action);

// Arithmetic operators
var id = 7 + 3; // 10
id = id - 2; // 8
id = id * 4; // 32
id = id / 2; // 16
console.log(id); // 16

// Complex data types (objects with properties)
var fiveDs = ["Dodge", "Duck", "Dip", "Dive", "Drive"]; // Array (numbered list)
console.log(fiveDs); // print content
console.log(fiveDs.length); // get length
console.log(fiveDs[0]); // get particular item

// Other
// - Object
// - Function
// - Date
// - DOM - Document Object Model
