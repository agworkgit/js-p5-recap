// Using the 'void' (expression) operator

void console.log("sample void"); // works like a function but does not return anything
// evaluates what's between parentheses but returns nothing

function x() {
  // no return, still returns 'undefined'
  // a functions must always return something...
  let a = 1;
}

let abc = x(); // undefined
